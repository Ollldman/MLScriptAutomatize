# **Учебный проект Скрипт-Автоматизация некоторых процессов в аналитике данных и машинном обучении. ModelForge**
## **Общая документация.**
### **1. Структура проекта:**
    - modules: 
Папка содержит реализацию основных модулей, реализующих вычисления, сбор и анализ данных, формирование отчетов и пр.
    
    - tests:
Папка содержит реализацию unit-тестов для программных модулей из папки modules.

    - app.py: Основной файл приложения.

    - settings.py: файл конфигурации pydantic-settings.BaseSettings с подгрузкой основных конфигураций приложения из .env


### **2. Требования к системе и среде запуска программы:**

- Необходимо  следующие данные для размещения их в файл конфигурации проекта:

Для корректной работы приложения необходимо подготовить и указать в файле `.env` (или в переменных окружения) следующие параметры:
---

##### 1. **Базы данных (Database Connection Strings)**

Приложение ожидает следующие строки подключения:

- `DB_CONNECTION` (str):
  - Строка подключения к основной базе данных (например, для хранения результатов анализа).
  - Пример: `postgresql://user:password@localhost/mydb`
- `DB_FOR_MODELS` (str):
  - Строка подключения к базе данных, **где будут храниться результаты моделей** (из `integration.db_storage`).
  - Пример: `postgresql://user:password@localhost/models_db`
- `AIO_DB_CONNECTION` (str):
  - Строка подключения для **асинхронной работы** с базой данных (например, через `asyncpg`).
  - Пример: `postgresql+asyncpg://user:password@localhost/mydb`

> 💡 **Примечание**: Эти строки должны соответствовать формату, поддерживаемому `SQLAlchemy`.

---

##### 2. **Доступ к Kaggle API**

Если планируется использовать загрузку датасетов с Kaggle:

- `KAGGLE_USERNAME` (str):
  - Имя пользователя на Kaggle.
- `KAGGLE_KEY` (str):
  - API-ключ, полученный в настройках аккаунта Kaggle.

> ⚠️ Эти параметры **не обязательны**, если вы не используете `load_from_kaggle`.

---

##### 3. **Настройки SMTP для отправки отчетов по Email**

Если планируется использовать функцию отправки отчетов по Email:

- `smtp_server` (str):
  - SMTP-сервер (например, `smtp.yandex.ru`, `smtp.gmail.com`).
- `smtp_port` (int):
  - Порт SMTP-сервера (например, `587` для STARTTLS, `465` для SSL).
- `sender_email` (str):
  - Email-адрес, с которого будут отправляться письма.
- `sender_password` (str):
  - Пароль или **App Password** от почтового ящика (рекомендуется использовать App Password для Gmail/Yandex).

---

##### 4. **Директория для отчетов**

- `reports_dir` (str):
  - Путь к директории, где будут сохраняться сгенерированные HTML/PDF отчеты.
  - Пример: `./reports`

---

#### 📁 Структура `.env` файла

Пример содержимого файла `.env`:

```env
# Подключения к БД
DB_CONNECTION=postgresql://user:password@localhost/main_db
DB_FOR_MODELS=postgresql://user:password@localhost/models_db
AIO_DB_CONNECTION=postgresql+asyncpg://user:password@localhost/main_db

# Доступ к Kaggle (если используется)
KAGGLE_USERNAME=your_kaggle_username
KAGGLE_KEY=your_kaggle_api_key

# Настройки SMTP
smtp_server=smtp.yandex.ru
smtp_port=587
sender_email=your_email@yandex.ru
sender_password=your_app_password

# Директория для отчетов
reports_dir=./reports

# Дополнительно
DEBUG=true
TEST_DATASETS_PATH=./datasets/
```

---

#### ✅ Что нужно сделать:

1. Создать файл `.env` в корне проекта.
2. Указать в нём все требуемые переменные.
3. Убедиться, что:
   - Базы данных доступны.
   - SMTP-данные верны и разрешают отправку.
   - Указанные директории существуют и доступны для записи.

